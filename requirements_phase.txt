# Phase-conditioning extras — not in the lerobot environment by default.
# Install with:
#   uv --project ../lerobot pip install -r requirements_phase.txt
# OR activate the lerobot venv first:
#   source ../lerobot/.venv/bin/activate && pip install -r requirements_phase.txt

vllm>=0.7
qwen-vl-utils
decord>=0.6
pydantic>=2.0
hydra-core>=1.3
rich
typer
pytest
pytest-cov
